[alembic]
# Directorio donde viven las migraciones (relativo al WORKDIR del container: /app)
script_location = migrations_platform
prepend_sys_path = .
version_path_separator = os

# sqlalchemy.url se define dinamicamente en migrations/env.py desde settings
# No se usa este valor directamente
sqlalchemy.url = driver://user:pass@host/dbname

[post_write_hooks]

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
