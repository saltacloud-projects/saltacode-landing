{
  "name": "@saltacode/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "test": "pnpm run build && node scripts/verify-build.mjs && node scripts/verify-server.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "7.2.4"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "sharp": "0.35.3",
    "typescript": "6.0.3"
  }
}
